import 'package:get/get.dart';

class InitialBinding extends Bindings {
  @override
  void dependencies() {
    // Inject global dependencies here (e.g., NetworkInfo, GetConnect, SharedPrefs, etc.)
  }
}
